{
  "name": "mineclon",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A voxel sandbox game (Minecraft clone) that runs in the browser.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "smoke": "node scripts/smoke-test.mjs"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "puppeteer-core": "^25.3.0",
    "vite": "^8.1.3"
  }
}
